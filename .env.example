DEEPSEEK_API_KEY="your key"
LLM_MODEL="model_name"
# 本地生图配置（默认即可）
# GEN_MODEL 支持别名 zimage / flux，或完整本地模型 id（如 black-forest-labs/FLUX.2-klein-9B）
GEN_MODEL="zimage"
GEN_DEVICE="cuda:4"            # 手动指定显卡，如 cuda:5；留空则用代码默认 cuda:0
OUTPUT_DIR="outputs"