# 拷成 .env 后填真值。.env 不进 git；本文件进 git，所以这里【只能】是占位符。
#
# 命名约定：字段名大写 = 键名（core/config.py 里声明什么字段，这里写什么键）。
#   LLM_*   大脑 / 子 agent 的 LLM
#   T2I_*   文生图工具（text-to-image）
#   I2I_*   图生图（将来）；视频同理，用能说清工具范围的前缀
# 键表的结构（类型 / 缺省 / 优先级）在 core/config.py——那是全项目唯一读环境的地方。


# ───── 大脑 LLM（DeepSeek，云端）─────
LLM_API_KEY="your key"
LLM_MODEL_ID="deepseek-v4-flash"
LLM_BASE_URL="https://api.deepseek.com"


# ───── plan 子 agent 的 LLM ─────
# 留空 = 沿用上面同名的 LLM_*，所以零配置就能跑（plan 工具总是注册）。
# 它们是【模型覆盖】，不是新的一组凭证；要让 plan 换个模型才填，例如
# 本地 vLLM 上的 Qwen3-8B（启动服务：scripts/serve_plan_model.sh，另见 README）：
# PLAN_MODEL_ID="qwen3-8b"
# PLAN_BASE_URL="http://localhost:8000/v1"
# PLAN_API_KEY="EMPTY"       # vLLM 不校验 key，但字段不能空（AgentLLM 会拦空值）
# PLAN_PROVIDER="vllm"       # 纯显示用（str(agent)），不参与任何分支判断


# ───── 生图（本地，无需 key）─────
# T2I_MODEL_ID 留空 = 无生图能力，工具集里就不注册 generate_image
# （干跑 / 只验提示词的用法）。要真出图，填 tools/t2i.py 里 MODELS 的键——
# 只能选 Z-Image-Turbo 或 FLUX.2-klein-9B，名字写错会 KeyError。
# T2I_MODEL_ID="Z-Image-Turbo"
# T2I_MODEL_DEVICE="cuda:0"
# T2I_OUTPUTS_DIR="outputs"


# ───── 以下全是【注释】，现在没有任何代码读它们 ─────
#
# 为什么注释掉而不是先写上：一个没有读取方的键 = 假配置。本仓库已经栽过两次
# （AGENT_WORKER_PROVIDER 被接受后无人读；LLM_MODEL 与 LLM_MODEL_ID 名字对不上
# 导致所有角色静默掉回 deepseek-chat）。键只在该有读取方的那一刻加进来。
#
# 什么时候会取消注释：第二个服务商真正接入时。
#
# 约定：凭证按【服务商】分，不按 agent 分——上面那组 PLAN_* 是个例外：
# 它是「换模型」用的覆盖项（留空即沿用同名 LLM_*），不是一组新凭证。
# 同一个 key 能调该服务商的多个模型，「plan 用 A、review 用 B」在同一家时
# 不需要新凭证；只有跨服务商才加一组，形状就是下面 LLM_LOCAL_* 那样。
# 角色 → (服务商, model_id) 的映射【不进环境变量】——env 是一维的，
# 那种二维结构写在代码表里（约定见 README）。
#
# 第二个服务商：本地 vLLM 起的 Qwen3-8B LoRA，OpenAI 兼容。
# vLLM 不校验 key，填个占位串即可，但字段不能空（AgentLLM 会拦空值）。
# LLM_LOCAL_BASE_URL="http://localhost:8000/v1"
# LLM_LOCAL_API_KEY="EMPTY"
# LLM_LOCAL_MODEL_ID="qwen3-8b-lora"
#
# 将来的生视频工具照此办理（前缀换成它自己的范围）：
# VIDEO_BASE_URL=""
# VIDEO_API_KEY=""
