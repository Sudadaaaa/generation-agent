DEEPSEEK_API_KEY="your key"
LLM_MODEL="deepseek-v4-flash"
# 其余配置全部走命令行参数：--llm / --model / --output-dir
# GPU：本地 LLM（qwen）固定逻辑 cuda:0、生图固定逻辑 cuda:1；
# 物理卡映射在 main.py 顶部的 CUDA_VISIBLE_DEVICES 里改（换卡只改那里）。
